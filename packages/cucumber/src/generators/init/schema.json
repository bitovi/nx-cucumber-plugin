{
  "$schema": "http://json-schema.org/schema",
  "$id": "CucumberInitSchematic",
  "cli": "nx",
  "title": "Add Cucumber Configuration to the workspace",
  "description": "Add Cucumber Configuration to the workspace.",
  "type": "object",
  "properties": {
    "skipPackageJson": {
      "type": "boolean",
      "default": false,
      "description": "Do not add dependencies to `package.json`."
    }
  }
}
